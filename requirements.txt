# requirements.txt
# Dependencies for Air Writer Project

# OpenCV for computer vision tasks and camera handling
opencv-python>=4.5.0

# MediaPipe for real-time hand landmark and gesture detection (the core library)
mediapipe>=0.10.0

# NumPy for efficient numerical operations and array handling (used extensively by OpenCV and for smoothing)
numpy>=1.20.0